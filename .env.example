# Brik Project - Environment Variables
# Copy this file to .env and configure for your local environment
# DO NOT commit .env to version control

# Android SDK paths (update based on your system)
# macOS default:
ANDROID_HOME=$HOME/Library/Android/sdk
ANDROID_SDK_ROOT=$HOME/Library/Android/sdk

# Linux default:
# ANDROID_HOME=$HOME/Android/Sdk
# ANDROID_SDK_ROOT=$HOME/Android/Sdk

# Windows default:
# ANDROID_HOME=%LOCALAPPDATA%\Android\Sdk
# ANDROID_SDK_ROOT=%LOCALAPPDATA%\Android\Sdk

# Java Home (automatically set by mise, but can override here)
# JAVA_HOME=/path/to/java

# Node options for large projects
NODE_OPTIONS=--max-old-space-size=4096

# Metro bundler port (default 8081, can change if needed)
# RCT_METRO_PORT=8081
